##━━━━━━━━━━━━━━━━━━━━━━━━━━
##       テンプレートファイル(SceneXhtml.vm)
##     Velocity テンプレート言語 (VTL) で記述       
##━━━━━━━━━━━━━━━━━━━━━━━━━━
<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE html>

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="ja" lang="ja" xmlns:epub="http://www.idpf.org/2007/ops">
<head>
<meta charset="UTF-8"/>
<link href="${style}" rel="stylesheet" type="text/css" media="all"/>
  <title> $!title </title>
</head>

<body class="standard03">

<p>
<br/><br/>
</p>

<p>
$contents
</p>

<p>
<br/><br/><br/>
</p>

<hr>

<p>
<br/><br/>
</p>

#if ($q0)
<p>
<a href="${q1}.xhtml">${q0}</a>
</p>
#end

#if ($q2)
<p>
<a href="${q3}.xhtml">${q2}</a>
</p>
#end

#if ($q4)
<p>
<a href="${q5}.xhtml">${q4}</a>
</p>
#end

#if ($q6)
<p>
<a href="${q7}.xhtml">${q6}</a>
</p>
#end

#if ($q8)
<p>
<a href="${q9}.xhtml">${q8}</a>
</p>
#end

#if ($q10)
<p>
<a href="${q11}.xhtml">${q10}</a>
</p>
#end

#if ($q12)
<p>
<a href="${q13}.xhtml">${q12}</a>
</p>
#end

#if ($q14)
<p>
<a href="${q15}.xhtml">${q14}</a>
</p>
#end

#if ($q16)
<p>
<a href="${q17}.xhtml">${q16}</a>
</p>
#end

#if ($q18)
<p>
<a href="${q19}.xhtml">${q18}</a>
</p>
#end

#if ($q20)
<p>
<a href="${q21}.xhtml">${q20}</a>
</p>
#end

<p>
<br/><br/>
</p>

<hr>

</body>
</html>


